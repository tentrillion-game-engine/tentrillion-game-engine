{
  "version": 10,
  "cmakeMinimumRequired": {
    "major": 3,
    "minor": 16,
    "patch": 0
  },
  "$comment": "CMakePresets.json file for building the TenTrillion.",
  "configurePresets": [
    {
      "name": "default",
      "displayName": "Default Config",
      "description": "Default Config using the Ninja Build System",
      "generator": "Ninja",
      "binaryDir": "${sourceDir}/build/default",
      "environment": {
        "PATH": "$env{HOME}/ninja/bin:$penv{PATH}"
      }
    },
    {
      "name": "windows-only",
      "inherits": "default",
      "displayName": "Windows-only configuration",
      "description": "This build is only for Windows platforms.",
      "condition": {
        "type": "equals",
        "lhs": "${hostSystemName}",
        "rhs": "Windows"
      }
    }
  ]
}